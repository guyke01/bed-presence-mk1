name: Release

on:
  release:
    types: [released]

jobs:
  publish:
    permissions:
      contents: write
      pull-requests: write
      pages: write
      id-token: write
    uses: ElevatedSensors/workflows/.github/workflows/firmware-publish.yml@main
    with:
      product-name: "bed-presence-mk1"
      build-type: "release"
    secrets:
      DOCS_REPO_TOKEN: ${{ secrets.DOCS_REPO_TOKEN }}
